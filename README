install vagrant:

 clone repo
 cd awxsetup
 vagrant up
 vagrant ssh awx.example.com
 vagrant ssh node01.example.com
 vagrant ssh node02.example.com
-----------------------------
machien req:
  1. ubuntu 18
  2. 2 cpu core, 4GB RAM
  3. install ansoible package:
  
    sudo apt-get install software-properties-common  && \
  sudo apt-add-repository ppa:ansible/ansible && \
  sudo apt-get update && \
 sudo apt-get install ansible

git clone https://github.com/vytec-co/awxsetup.git
cd awxsetup
./setup.sh
